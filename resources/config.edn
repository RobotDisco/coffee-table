{:database {:url #join ["jdbc:postgresql://"
                        "localhost:5432"
                        "/coffeetable"
                        "?user=coffeetable"
                        "&password=coffeetable"]}
 :migratus {:store :database
            :migration-dir "migrations"
            :migration-table-name "migratus_migrations"
            :db ^:ref [:database :url]}
 :webserver {:host "localhost"
             :port 3000}}
